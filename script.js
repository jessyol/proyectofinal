async function addTransaction(e) {
    e.preventDefault();
    
    
    console.log('El botón "Añadir Transacción" fue presionado.'); 

    if (descriptionInput.value.trim() === '' || amountInput.value.trim() === '') {
        
    }
}